{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PHP",
            "type": "php",
            "request": "launch",
            "port": 9000,
            "serverSourceRoot": "${workspaceRoot}",
            "localSourceRoot": "${workspaceRoot}"
        }
    ]
}